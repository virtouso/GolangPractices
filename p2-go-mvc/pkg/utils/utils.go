package utils

import (
	"encoding/json"
	"io/ioutil"
    "net/http"
)

func ParseBody(r *http.Request, x interface{},
	body, err, _ ioutil.rReadAll(r.body); err==nil{
		if(err:= json.Unmarshall)

	}
)